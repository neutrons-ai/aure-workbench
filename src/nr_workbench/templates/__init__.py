"""Scaffold templates shipped as package data.

This module exists only so ``importlib.resources.files`` can locate the
directory; the templates themselves are plain files rendered by
:mod:`nr_workbench.project.render`.
"""

from __future__ import annotations
