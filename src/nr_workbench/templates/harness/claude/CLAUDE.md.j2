# Claude Code Instructions

This is an **nr-workbench** project for neutron reflectometry analysis at
{{ facility }} {{ instrument }}. It was scaffolded by `nrw init`.

Everything that governs work here is in the two files below. This one is a
pointer, deliberately: `AGENTS.md` is read directly by the other assistants
this project supports, so keeping the substance there means one copy rather
than one per tool, and no way for them to drift apart.

@.github/copilot-instructions.md

@AGENTS.md

**To change how an assistant works in this project, edit `AGENTS.md` or
`.github/copilot-instructions.md` — not this file.**
